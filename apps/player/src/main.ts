const app = document.getElementById("app");

if (app) {
  app.innerHTML = `
    <section class="hero">
      <h1>WebBlackbox Player</h1>
      <p>Load and inspect .webblackbox recordings from local files.</p>
    </section>
  `;
}
